spring:
  application:
    name: order
  cloud:
    config:
      discovery:
        enabled: true
        service-id: CONFIG
      profile: dev
    stream:
      bindings:
        input:
          group: order
          content-type: application/json
logging:
  level:
    com.sell.order.mapper: debug
    org.springframework.cloud.bus: debug


